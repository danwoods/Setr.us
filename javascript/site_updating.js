function setSongTime(songId, time){
  alert("songId = " + prevPlayingSong + " time = " + time);
  /*
  if(xmlHttp){
      //alert("sever is available");
      //if yes try
      try{
        var params = ("songId=" + songId + "&time=" + time);
        //alert("parameters being sent to the server are " + params);
        xmlHttp.open("GET", "php/setSongTime.php?" + params, true);
        xmlHttp.send(null);
        }//end try
      catch(e){
        alert("Can't connect to server: \n" + e.toString());
        }//end catch
      }//end if xmlHHttp
  */
  }//end function

